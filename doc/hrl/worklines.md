# Worklines plan (started 2026-07-16, grilled) — parallel tracks + delegation

This chat (the planning chat) owns this file, sequencing, and hand-offs; implementation
runs in delegated chats/agents (see `.claude/skills/delegate`).

**This file is the registry**: current status + who owns what + delegation history.
Full evidence/numbers/mechanism detail for each workline lives in its canonical doc —
`A1a_deploy_plan.md` for WL-B, `A1a_plan.md` for WL-D, the A1 findings ledger (research KB) for WL-C,
ADR-0005 for WL-E — this file summarizes and points there, it does not restate.

> **Closed verdicts and delivered hand-off prompts moved out (2026-07-28).** The full
> WL-C and WL-E verdicts, and the WL-B0a/WL-B0b hand-off prompts kept for provenance,
> now live in the author's research knowledge base. This file is the live registry:
> who owns what, what is in flight, and the standing rules.

## Decisions locked (2026-07-16 grill)

1. **Sequencing: WL-C verification gates the WL-D batch.** The HIRO-relabel hold-bias
   suspect gets verified FIRST (cheap: code analysis + one relabel-off control run).
   If verification drags past ~2 days, WL-D launches anyway, downgraded to
   lever-validation probes. (Resolved 2026-07-16 — WL-C verdict, research KB.)
2. **WL-D levers**: `ll_cadence_coef` sweep; `hl_cot_coef` refinement; stand-still
   gating term; A0 gait terms mirrored into the LL intrinsic one at a time; lower-body
   posture/ankle-roll anchor; heel-to-toe roll-over/push-off (research-first). All
   implemented and run — see `A1a_plan.md` "WL-D batch results" / "Arm 6" / "Arm 10".
3. **Budget: full 10k every arm** (10001 iters, 4096 envs, seed 42, `model_10000`),
   R-round protocol: aggregate bench + `--eval-cmd-vx` holds + `[HOLDDIAG]` + goal
   probe + replay look. Naming per the deviation-tagging convention.
4. **Delegation: `/delegate` skill** (hand-off prompt generator + model picker) plus
   background subagents from the planning chat for small verifiable tasks.

## Worklines status

| WL | What | Owner chat | State |
|---|---|---|---|
| **A** | Planning, sequencing, doc/gate ownership, hand-offs | THIS chat | live |
| **B** | Deploy pipeline (plan: `A1a_deploy_plan.md`). Split 2026-07-20 into **B0 = A0-first hardware (PRIORITY)** and **B1 = A1 keeper** — same shared `deploy/` tree, one bridge, one flight recorder; kept inside WL-B rather than a new workline for exactly that reason. Both tracks' full gate ladders, defect history, and hardware-session logs live in `A1a_deploy_plan.md`; only a rolled-up status is kept here. | WL-B chat(s); ONE deploy chat at a time (shared `deploy/` tree) | **B0 (A0-first):** first real-hardware session 2026-07-23 found two separate causes of the "drunk stumbling" and disentangled them. (1) The safety-filter whole-body joint-limit hold arms on real hardware (fires on overshoots as small as 0.002 rad) — the same defect fixed for A1 on 2026-07-16, never ported to A0. **Ported + hardware-confirmed 2026-07-23 (WL-B0a)**: 225.8 s of continuous free walking, filter never engaged once. (2) A persistent ~3-5° backward stand lean, independent of the filter — **decomposed 2026-07-23/24 (WL-B0b) into two near-even causes** via three independent pitch estimates (IMU, kinematics, statics): a constant encoder→attitude map error and excess knee droop from unmodelled leg mass. **BOTH NOW DIAGNOSED; no cause remains unidentified.** (a) The **2026-07-28 inclinometer session vindicated the IMU by direct measurement** (pelvis −6.80° vs IMU −6.30°, agreeing to 0.50°, while the encoder feet-flat FK said −3.16°; torso and pelvis read identically, killing the pelvis-vs-torso question physically). Assumption-free core: with true attitude + measured encoders the model puts the foot **2.74° toe-up while it is provably flat on a flat floor**. That splits into **~0.9° foot-sole wedge** (both soles measured thicker at the FRONT → both feet toe-up 0.74°/1.11°, tilting the body backward; the flat capsule sole cannot represent it) + **~1.8° leg-encoder zero error** (deploy `joint_offset`, Spec B, no retrain; a vendor re-zero was considered and rejected as risky/unverifiable on the parallel A/B ankle). Refined model over 2 sessions: `p_IMU = 1.132*p_kin − 0.0394` (r=0.973). **Do NOT recalibrate the IMU** — it is the honest sensor. (b) Leg mass **resolved 2026-07-24**: real leg 18.60 kg vs the model's 15.40 kg. Model fix (leg mass + sole geometry) staged in `docs/adr/0006`, held so it does not rebase the plant mid-WL-D-batch. Also resolved: the E-stop out-of-process-kill question (LAN-cable pull verified on hardware). **NEXT (approved 2026-07-28): validation experiment V1 — cut leg mass ALONE, retrain A0 at the deployed baseline's protocol, re-measure the lean on the robot, then direct the rest from that result** (the leg mass is the only *weighed* error; the wedge and encoder zeros are inferred). V1 is diagnostic because the two components move orthogonal metrics: leg mass moves `p_kin` (predict −0.0345 → ~0) while the residual `p_IMU − p_kin` must **stay** at ~−0.032 (the control); visible lean should roughly halve, ~3.8° → ~2°, NOT go to zero. Three specs written against `docs/adr/0006`: **A** = flip `biased=True` on the shared `joint_pos` obs term (the encoder-bias DR is active at ±0.86° and its actuation half is modelled, but the observation half is off, so the real failure mode — sensors contradicting each other — is never trained; v3-cut change, still PROPOSED, rides the later rebase); **B** = deploy `joint_offset` for the ~1.8° encoder-zero error, applied on BOTH read and write, free and testable in a 60 s stand (pass criterion: residual −0.048 → ~−0.016, the wedge floor, NOT zero); **C** = set `mode_pr` explicitly (H1-2's ankle is a parallel A/B mechanism; we get PR mode only from an IDL zero-init default, and a silent flip to AB would halve commanded ankle pitch and inject a spurious roll on the tightest joint). **B + C IMPLEMENTED AND SIM-VALIDATED 2026-07-31 (WL-B0c)**: `joint_offset` plumbing (read in `unitree_articulation.h`, write in `State_RLBase.cpp`/`State_RLHRL.cpp` after the safety clamp) is inert when absent/zero (pytest 55/55 incl. 6 new tests, sensitivity 22/22) and round-trip-correct on the live sim bridge — a one-sided negative control leaked the full injected test offset, the shipped symmetric code leaked only 2.6% (PD-droop noise). No numeric offset committed yet (stays all-zero, config carries the plumbing only); pending the user's diff review, then the hardware session. **Validation experiment V1 (leg mass ALONE) run and FALSIFIED, 2026-07-31 (WL-B0d)**: retrained A0 at the deployed baseline's exact protocol (+ an rs8 same-family comparator, both agreeing to <0.001 on tracking metrics — ruling out seed luck) on the mass-corrected plant, re-measured the stand on hardware via the same inclinometer method as 2026-07-28 — **upper-body lean, thigh, and shank angles all came back identical to the pre-cut baseline. Leg mass is not the driver of the lean.** Corrects a piece of this ADR's own framing: leg-mass mismatch is no longer valid grounds to dismiss comparisons against other sims/checkpoints **as an explanation of the lean specifically** (the general modeling-fidelity point about other H1-2 sims running 21% light still stands on its own). Sim-vs-real joint-angle comparison (`meas_q`, all 12 leg joints) shows tight (~1-3°) agreement everywhere, including hip_pitch — doesn't isolate the user's hip_pitch hypothesis, since that comparison tests posture-tracking fidelity, not encoder-zero bias; the geometric (feet-flat FK) method is the right tool there and per the ADR can't identify the per-joint split from the lumped total alone. Full numbers, corrected raw_q/meas_q note, and the joint table → the deploy journal in the research KB. Redirects the investigation toward Spec B / encoder-zero, away from further leg-mass work. **Probe P1 (2026-07-31, WL-B0e) closed the quantitative gap V1 left open** — new `scripts/play.py --probe-lean` measures d(lean)/d(encoder offset) in sim, no retrain, no hardware. Feeding the independently measured 2.74° chain discrepancy through the measured gain predicts the observed stand lean to **8%** (−4.12° predicted vs −3.83° observed), sign included; superposition across joints holds to 2.3%; and the offset is constrained to be **knee-dominant or roughly even, NOT ankle-concentrated** (a purely ankle-located 2.74° over-produces the lean ~1.4x). Also found: the **wedge and any ankle encoder zero are the SAME channel** (a toe-up sole is kinematically an `ankle_pitch` offset), so the Amendment's habit of summing them separately overstates item 2's benefit. And the lean is **not purely a plant property**: the rs8 policy's chain gain is 18% lower than the deployed rs20's, giving the pre-registered prediction that **swapping to rs8 cuts the lean ~0.9° to about −3.2°**. The V1 leg-mass XML edit was found still applied and reverted. Tables → `docs/adr/0006` "Probe P1". Also **open**: the **L/R leg asymmetry** (wedge checked, does NOT explain it: differential 0.375°, wrong sign); a second G3.3 walk + the user's qualitative sign-off. Full detail → `A1a_deploy_plan.md`. **B1 (A1 keeper):** candidate LOCKED = `arm4d` (`ll_energy_coef=0.05`) — passes the live bridge (2026-07-22, all directions) and **replicates on a second seed (2026-07-24, WL-D combo batch)**; D2/old keeper/arm3/arm4c/arm5 all ruled out (twitch, not clearance — see WL-D row). **E1's velocity blocker RESOLVED 2026-08-01**: `delta` mode cancels absolute base velocity, so the LL needs only the within-window IMU increment — measured in sim at vx 0.020 / vy 0.025 m/s (ship band), **no retrain, no goal-space rework**; requires the integrator in `run()` (1 kHz, not 50 Hz) + a torso→pelvis IMU correction, and leg-FK for the still-absolute height. The velocity-blind-LL retrain sweep is retired unrun. Remaining velocity-side work is the **HL**, which does need absolute velocity. **Arm 1 (`hl_obs_vel=False`) DONE 2026-08-02: the blind HL walks (0 falls) but costs 2x linear tracking (err_vx 0.064→0.127, replicating the 2026-06-30 velobs A/B prediction exactly), +26% act_rate and +24% CoT; the probe shows BOTH levels degrade (LL reach +45-52% via co-training coupling) while yaw is flat, the internal control.** **Open HL choice (2026-08-03): ship blind, or leg-odometry velocity for the HL alone.** `[LEGODOM]` bench built; leg-odom is drift-free (instantaneous stance-foot kinematics) and the HL fires at 6.25 Hz so it filters heavily. Measured c-averaged HL-consumed error **0.074 vx / 0.049 vy at the physics rate** = MARGINAL band ⇒ per pre-registration, decided by a retrain probe (`hl_obs_vel=True` + `goal_state_noise` emulating RMS 0.074/0.049, bias -0.038), NOT by the bench. Ruled out along the way: `tau_est` soft stance (net regression, robust to T), and contact detection generally (a contact ORACLE buys ~nothing — 85% of steps are single-support where there is no ambiguity). ⚠ **Both a DROP verdict and the `tau_est` arm were produced against a 50 Hz-aliased baseline** — finite-differencing/integrating fast signals at the control rate is worth 2-4x on this robot and has now flipped two verdicts in one day. Deploy will run **fixed upper body** first/possibly always, which may void arm 1's worst (upper-body) regressions — under analysis. **Deploy C++ estimator LANDED + first bridge run 2026-08-03** (`hrl/base_state.h`, 1 kHz integrator, leg-FK height, `est_*`/`gt_*` telemetry; flags default OFF ⇒ back-compatible). Bridge verdict: **keeper runs clean (0 falls, 0 tilt triggers)**, but ⚠ **the estimator reads 5-8x worse than sim** (vx 0.115/vy 0.205 vs 0.020/0.025) and **`run()` does not sustain 1 kHz** (dt p90=max=10.00 ms + known 2-6 ms lag) ⇒ the "no retrain, ship band" claim is PROVISIONAL. Height FK bias +0.033 m, reproducible to 0.6 mm across two gaits ⇒ static anchor bug, cheap fix. A fall during the first run was traced NOT to the new code but to a parked WL-D arm-6 export (`rolloverfix0p5`) sitting in the deploy dir since 2026-07-29 — **always md5-verify the deployed ONNX against the run dir; `run_path` metadata reads `local` for training-time exports.** **READINESS PIPELINE DELIVERED 2026-08-04 — `scripts/deploy_readiness.py`, one command → one verdict** (exit 0 GO / 3 CAVEAT / 1 NO-GO / 2 INFRA), chaining provenance → sim → replica → bridge (candidate + A0 control, fingerprint-cached) → analyze, fail-closed (never GO from an unscored stage; falls read from CSV evidence, never FSM transitions). **All four B1 open items are closed, and three of them were closed by RETRACTING a prior finding:** (1) ⚠ **the "10 ms loop tail" never existed** — `t` in the safety CSV was `tick_counter × nominal dt` (not a clock) and `setprecision(4)` quantised it to 0.01 s bins past t=10 s; below 10 s the same files show only {1,2} ms, exactly what `log_every_=2` predicts, and the three "mean rates" were `trig_joint` in disguise. **`run()` measured at 990.4 Hz** via a new `t_wall` column. (2) ⚠ **the estimator's 5-8x sim-vs-bridge gap was a broken ground-truth reference, not an estimator or rate failure** — `gt_dvel` subtracted a lever arm from `v_gt_b`, which is already the PELVIS velocity (the bridge publishes `framelinvel` on body `pelvis`, `framepos` on the imu SITE); the spurious term cancelled against the identical one in `est_vel`, so telemetry scored the UNCORRECTED reconstruction (bench 0.227 ≈ bridge 0.205). Fixed → **vx 0.0139 / vy 0.0288, inside the ≤0.05 SIM band ⇒ ship-band claim CONFIRMED on the bridge, PROVISIONAL lifted, no retrain.** Attribution measured, not assumed: code fix 4.2x/5.0x with plant held constant, plant switch a further 5.8x/4.8x — both matter, an earlier "the reference bug explains it all" was overstated. (3) **`trig_joint` root-caused**: it is ankle-roll COMMAND saturation, and the deploy limit ±0.2618 is IDENTICAL to the training XML — the policies genuinely command 2.1-2.7x past the stop (rolloverfix peaked +1.999 rad past a ±0.2618 joint) because MuJoCo silently clamps qpos so training never charges for it. ⚠ **auto-memory `deploy_sim_blind_to_joint_hold` is STALE** — it describes the pre-2026-07-16 semantics (measured joint out of range), which MuJoCo indeed cannot produce; the current command-clamp semantics are observed by the bridge perfectly. (4) analyzer crash fixed (dt from row spacing, not `median(diff(t))` = 0), plus `bridge_session.py` was handing HRL runs the wrong CSV entirely. **Also: height anchor fixed** (`nominal_root_height` and `fk_h_nominal_` referenced two DIFFERENT poses; bias +0.0331 → +0.0045 m, though the initial-stand residual 12.8 mm still misses the ≤5 mm bar). **Provenance gate** md5-identifies the deployed ONNX against a reverse index of all run exports and NAMES the source run, fails if it is not the requested one (replayed the 2026-08-03 parked-policy scenario: caught, exit 1, aborts before any process starts), stages via `--stage` + `exported/PROVENANCE.json`; `onnx_parity.py --onnx-dir` now scores the DEPLOYED file (it never did — W4 could not have caught this). **Plant switched to the shipped Unitree scene** (`scene_stress.xml`) after the user corrected a naming error: the file labelled "vendor reference" is a hand-made training-proximate variant, and the user's own A/B falsified my claim that its fictional free-base damper was the discriminator (it is the joint damping, 1 vs 0.001). ⚠ **Every reference number predating 2026-08-04 was measured on the retired plant and does NOT transfer** — re-baseline before comparing. pytest 59→**73**, sensitivity **31/31**. ⚠ **FIRST FULL-BATTERY RESULT: the keeper `arm4d` FAILS G2.2/G2.3 on the shipped plant.** Stands clean, walks 0.3 clean (7.1 m), walks 0.5 clean (12.5 m), then **falls on the 0.5→0 deceleration at t=77.4** (alpha 0.58→1.0) and stays down. **A0 on the identical binary/config/sequence is clean (0 falls, 19/19 transitions, alpha 0)** ⇒ per the A0-control methodology the fault is in the HRL path or the policy, not the shared layer. Same signature as the 2026-08-03 fall — the decel, not any hold — and the old 4-command sequence does NOT reproduce it (15 s hold before the decel passes, 30 s falls). Caveats: n=1; arm4d's "bridge-passed" status was earned on the RETIRED plant and this is its first shipped-plant measurement; longer-hold vs plant vs variance not yet separated (cheap next step: repeat at 15 s vs 30 s on both plants). **Until then arm4d is NOT a validated hardware candidate on the shipped plant.** **Open for B1:** separating that; the ~+0.018 m unattributed height term; the L/R + backward cmd-0 drift now quantified by a new metric (keeper 0.232 m at 159°, A0 0.178 m at 112° — both left, pointing at plant/model rather than policy). Full detail → `A1a_deploy_plan.md`. |
| **C** | HL held-command fix: verify HIRO-relabel hold-bias suspect, fix if real | closed | **CLOSED 2026-07-16 — FALSIFIED.** FALSIFIED. Full verdict and evidence → the worklines archive + A1 findings ledger (research KB). |
| **D** | Reward/gait lever batch (energy, gait quality, symmetry) | WL-D chat(s) | **Batch winner: `arm4d` (`ll_energy_coef=0.05`)** — CoT −48%, calmest A1 in the batch, now the B1 deploy candidate (see WL-B row — validated live 2026-07-22). S4′'s honest disconfirmer fired: A0+energy matches the energy saving without regressing tracking, so "the hierarchy buys energy" is unsupported by this batch. Arms 6 (heel-toe push-off, 2 formulations + a sign-bug found/fixed) and 10 (L/R gait symmetry, 2 formulations) shipped and trained; neither clears the winner bar cleanly (tracking cost / no measurable effect). **Arm 6 update (2026-07-23): a second, cross-formulation ankle-roll (inversion/eversion) confound was found alongside BOTH formulations' pitch motion (worst case ratio 1.78, neither formulation reads/rewards ankle_roll at all - a whole-body-policy side-channel, not a reward-shape bug) - an `ll_posture_anchor_ankle_roll=True` anchor test on formulation A (coef=0.1) killed it cleanly (roll/pitch ratio 1.59→0.006) while barely touching the pitch motion (+6.3°→+5.8°) and improving tracking/CoT too. Current best arm-6 candidate: formulation A + coef=0.1 + ankle-roll anchor.** **Arm 6 CLOSED/PARKED 2026-07-29**: a third formulation (C, contact-sequence heel-to-toe, redesigned to a position-based contact classifier after a geometry bug was found in the first per-geom grouping) was built and trained, but both variants (with/without the ankle-roll anchor) satisfy the reward via a static forefoot-loading bias rather than a genuine phase-timed rollover (full-stance pitch ROM stayed ~1° in both). Set aside as a gait-polish item, not a blocker — full saga → journal. Priority shifts to WL-B hardware. Full batch tables (g)/(h)/(i) + Arm 6/Arm 10 sagas → research KB `a1a-experiment-journal.md` (migrated out of `A1a_plan.md` 2026-07-28). **Combo batch CLOSED (combos 1-12 + arm4d seed-2; full record + tables → research KB `a1a-experiment-journal.md`).** arm4d **replicates on seed 123** (noise floor now measured: ±5.8% CoT / ±1.2% act), so the deploy candidate no longer rests on one seed. **No combo beat arm4d; non-additive in all 9 cases.** Smoothness: `ll_action_rate_coef` 0.02→0.05 (combo1) is the ONLY lever that moves the blocker (act 0.923→0.837, lowest of any A1) but stays ~30% over A0's 0.644. **`ll_joint_acc_coef` implemented then REJECTED at both doses** (2.5e-7 / 5e-7): doesn't move the arms share, worsens `ub_arm_vel` to 0.396, non-monotonic on act, and breaks zero-command standing (touchdowns 142→346/5325). **Action-rate root-caused** via the new `--diagnose-action-rate` probe: the HL's 6.25 Hz goal step is real but worth only −4.2%; the rest is a uniform floor +38% over A0, **arms-heavy** (arms share 0.304 vs A0 0.115) → the surviving suspect is A0's `pose`/`variable_posture` (its largest reward, tight per-joint arm stds), which A1 only approximates. **Two stepping-in-place causes separated:** the A1 phantom goal at cmd 0 (fixed by `ll_stand_still_coef` — combo7 = arm4d that also stands cleanly, touchdowns→128 floor at ~zero deploy cost) and footclear's gait-character carry-over (combos 8/9 step 234/340; NOT rescued by stand_still) → footclear dropped. Also measured: **every A1 lands 1.8-2.8× harder than A0** (521-798 N vs 283 N). Next: arm-posture pressure (knob exists, needs a launcher flag), not another whole-body penalty; `ll_action_rate_coef` 0.08/0.12 at ≥2 seeds. Unmirrored A0 terms remaining: `body_ang_vel`, `soft_landing`. |
| **E** | Training-nominal realism + IsaacGym parity audit | closed | **CLOSED 2026-07-17.** Full verdict → the worklines archive (research KB); decision → ADR-0005 Amendment 2. |

## Backward lean: remaining steps, in order (WL-B0, opened 2026-07-31)

Model v3 is **NOT being cut** (the user 2026-07-31): V1 showed leg mass moves nothing on hardware,
and rebasing the plant would invalidate every current LL-combo checkpoint for no measured gain.
The leg-mass numbers stay documented as correct-as-weighed but non-causal. Everything below is
free of a retrain except step 5.

1. **[DONE 2026-07-31] Probe P1 — measure the transfer function in sim.**
   `python scripts/play.py Unitree-H1_2-Flat --checkpoint-file <deployed A0 .pt> --num-envs 64
   --eval-seeds 2 --eval-cmd-vx 0 --probe-lean 400 --probe-lean-bias "-0.04,...,0.0317"
   --probe-lean-joints "all,ankle_pitch,knee,hip_pitch"`. Results + interpretation →
   `docs/adr/0006` "Probe P1".

2. **Per-joint identification of the offset — hardware, ~30 min, no policy change.**
   The one measurement everything else waits on: Spec B is implemented but the feet-flat
   constraint pins only the chain SUM, and P1 shows the split matters (ankle gain 1.684 vs hip
   0.733, a 2.3x spread). Procedure: inclinometer on pelvis / thigh / shank / foot in the
   standing pose **and in a second, distinctly different pose**. Differencing two poses cancels
   the cast-face-vs-kinematic-axis offsets that make a single reading carry ±2° (the shank's own
   two faces disagree by 2.1°); the standing pose then gives the absolute offset. Use the
   **passive hang** as the second pose — zero contact and zero load removes the sole wedge and
   any compliance from the comparison outright. Compare the derived true joint angles against the
   reported `q` from a concurrent `read_all_joints` log. **P1's falsifiable prediction: the
   offsets come out knee-dominant or roughly even, not ankle-concentrated.**

3. **Spec B with the measured numbers — hardware, 60 s stand.** Code is landed and sim-validated
   at zero (WL-B0c); only the `joint_offset` values are missing. Fill them from step 2. Pass
   criterion unchanged: residual `p_IMU − p_kin` moves −0.048 → ≈ **−0.016 rad**, the sole-wedge
   floor, **not** zero. Overshoot past zero = wrong sign. No movement = encoder-zero falsified,
   revert Spec B and report the whole residual as geometry.

4. **Policy-swap control (the user's suggestion) — hardware, free, run in the same session as 2 or 3.**
   Deploy `a0_v2_optB_baseline` (rs8) instead of the rs20 keeper and re-measure the stand.
   Pre-registered: **−3.2°, i.e. ~0.9° less lean than rs20's −3.83°** (P1 chain gains 1.031 vs
   1.263). Detectable on the IMU, marginal on the inclinometer (±0.5°). A null falsifies the gain
   model and says the lean is set by something outside this channel. Note the V1 legmass
   checkpoints are NOT valid comparators here — they trained on the heavy plant, now reverted.

5. **[DONE 2026-07-31, the user's call — overturned my "defer it" position] Spec A trained and
   measured: `a0_biasedobs_rs20_s42`.** `biased=True` on the shared `joint_pos` term (actor AND
   critic — the bias is a hidden per-env constant that changes what the actor does, so a critic
   given the true angle but not the bias is worse off, not privileged), deployed keeper's exact
   protocol, `bias_range` untouched at ±0.015 so the flag is the only variable. **P1 re-run: the
   chain gain fell 1.263 → 0.772, crossing below unity — the baseline AMPLIFIES a static
   calibration offset, this policy ATTENUATES it.** Per-joint: ankle 1.684→1.231, knee
   1.287→0.835, hip 0.733→0.245. Unbiased sim stand unchanged (−0.0113 vs −0.0115), so its
   intrinsic posture is identical and only offset-rejection improved. **Free**: benchmark all
   inside the 2-seed spread (`action_rate` −4.8%, `err_vx` −2.7%, `fall_rate` 0.0 both).
   **Pre-registered: −2.76° on hardware, ~1.4° less lean than the keeper.** Mitigation, not a
   cure — still transfers 61% of the offset, so Spec B still owns removing the input.
   ⚠ `biased=True` is live in the working tree on the SHARED term: **revert before any WL-D
   training** or that batch silently diverges from `arm4d`.

   **HARDWARE READ-OUT 2026-08-03: FALSIFIED, Spec A REJECTED as a default.** Inclinometer 83.4°
   (6.6° lean) vs 83.1-83.2° baseline = 0.2-0.3°, inside the ±0.5° resolution; IMU −5.51° vs the
   keeper's −6.13° = 0.62° against 1.36° predicted, inside the 1.0° between-session spread. **And
   behaviourally worse**: still-fraction at zero command 76.0% vs 91.6%, mean `max|dq|` 0.243 vs
   0.057 = **4.3x the joint motion**, matching the operator's observation of it swinging into steady state
   and taking stabilising steps. Sim showed none of it. **Root cause: `history_length=1`** — a
   memoryless policy cannot infer the bias, so the only strategy under biased obs is to
   down-weight `joint_pos` and lean on `projected_gravity`, which is free against sim's
   ground-truth gravity and expensive against a real IMU with lag, noise and accel coupling. It
   trades encoder fragility for IMU fragility. **Encoder-bias DR is only exploitable by an
   architecture that estimates the hidden parameter from a history = A2/RMA.** Do not retry on a
   memoryless policy; do not widen `bias_range` (deepens the same trade).
   **⚠ METHOD CONFOUND FOUND, affects everything above:** across the three deployed policies,
   **stillness predicts stand lean at r = −0.96 while the P1 chain gain predicts it at r = −0.25**
   and gets the rs8-vs-biasedobs ordering backwards. A fidgety policy settles less drooped and
   reads less lean. **This retracts step 4's rs8 result as evidence** (rs8 was the least still,
   62.8%). P1's *within-policy* measurement is untouched; using its gains to compare *different*
   policies on hardware is not valid. Report stillness beside every lean number from now on.

6. ~~Widen `bias_range`~~ **— cancelled by the 2026-08-03 read-out** (it deepens the IMU-fragility
   trade). Still deferred: item 2 (sole-wedge XML), whose expected benefit P1 revised DOWN since it
   shares the ankle channel. **Spec B (step 3) is now the priority**, and its advantage is
   structural rather than just cheap: it is a **within-policy** intervention — same policy, same
   stance behaviour, only the calibration changes — so the stillness confound does not apply to it.

7. **Superseded rationale, kept for provenance: "now data-backed, widen `bias_range`".** At ±0.015 i.i.d. the 3-joint chain
   sum has std 0.015 against the real 0.0317, so the true defect sits at ~2.1σ (~1.5% of envs) —
   the policy is generalising to a case it almost never sees. A per-leg *correlated* component
   would match the real defect's structure better than i.i.d. Only pursue if the hardware
   read-out confirms the gain drop but falls short. Also still deferred: item 2 (sole-wedge XML),
   whose expected benefit P1 revised DOWN since it shares the ankle channel.

**Cheap parallel lever, if WL-D is retraining anyway:** P1 measured the ankle channel's gain at
**1.684 > 1**, i.e. the closed loop *amplifies* a static ankle offset rather than rejecting it
(hip is attenuated at 0.733, so this is a CoP/support-shift effect, not simply weak control).
Raising the `body_orientation_l2` weight (already on `torso_link`,
`config/h1_2/env_cfgs.py:150`) attacks the **gain** rather than the input, and would shrink the
transfer of *any* calibration error. Zero marginal cost on a retrain already happening — but do
not do it speculatively ahead of step 2, and it is an RQ2-shared change (see the A0-comparison
cleanliness rule).

## Standing rule for the planning chat (WL-A)

When this chat catches itself deep in implementation (multi-file edits, debugging
loops, running trainings), stop and emit a hand-off instead: that work is usually too
easy for the expensive model and burns the planning context. Small, objectively
verifiable one-offs may go to background subagents (sonnet) directly.
